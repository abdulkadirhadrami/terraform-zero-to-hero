{
  "name": "Terraform Codespace",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "postCreateCommand": "curl -fsSL https://releases.hashicorp.com/terraform/1.6.6/terraform_1.6.6_linux_amd64.zip -o terraform.zip && unzip terraform.zip && sudo mv terraform /usr/local/bin/ && sudo apt-get update && sudo apt-get install -y awscli"
}
